let edad = 20;
let tieneDui = true ;
let puedeVotar = edad >=18 && tieneDui ? "Puede votar": "no puede votar";
console.log(puedeVotar)

let age = 45;
let esMiembro = true;

let descuento = age >= 30 || esMiembro ? "aplica el descuento" : "no aplica el descuento ";
console.log(descuento)

let Edad = 15;
let Altura = 1.62;
let puedeSubir = Edad >= 12 && Altura >= 1.60 ? "Puede subir a la montaña rusa": "No puede subir a la montaña rusa";
console.log(puedeSubir)