import readline from 'readline';

const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});

/* rl.question("Primer numero ", (n1) =>{
    rl.question("segundo numero: ", (n2) =>{
        const suma = parseInt(n1) + parseInt(n2)
        console.log(suma)
        rl.close()
    })
}); */

/* rl.question("Insert Celcius: " , (celcius) =>{
    const fahrenheit = (parseFloat(celcius) + (9 / 5)) + 32
    console.log(`${celcius} celcius = ${fahrenheit} fahrenheit`) 
} );
 */


